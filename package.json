{
  "name": "moneroocean-stream-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix pwa install && npm --prefix pwa run build",
    "dev:pwa": "npm --prefix pwa run dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
